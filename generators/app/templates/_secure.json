{
  "database": {
    "mongo": {
      "db": "<%= dbname %>",
      "host": "<%= dbhost %>",
      "port": <%= dbport %>,
      "user": "<%= dbuser %>",
      "password": "<%= dbpassword %>"
    }
  },

  "authmaker": {
    "mongo": {
      "db": "<%= dbname %>",
      "host": "<%= dbhost %>",
      "port": <%= dbport %>,
      "user": "<%= dbuser %>",
      "password": "<%= dbpassword %>"
    }
  },

  "server": {
    "session_secret": "<%= sessionSecret %>"
  }
}
